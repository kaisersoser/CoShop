// Design-sync library entry — re-exports CoShop's UI components for
// the claude.ai/design converter (passed as --entry).
export { ShoppingList } from './components/ShoppingList';
export { CostFooter } from './components/CostFooter';
export { ItemRow } from './components/ItemRow';
